version: "3.5"

services:

  pushgateway:
    image: prom/pushgateway:v1.0.0
    container_name: pushgateway
    ports:
      - "9091:9091"

#  webserver:
#    build: webserver.Dockerfile
#    ports:
#      - "8080:80"
#    links:
#      - pme_app

  pme_app:
    build:
      context: ..
      dockerfile: ./test_related/pme-client.Dockerfile
    container_name: pme_app
    depends_on:
      - pushgateway
    #entrypoint: go test ./e2etest/e2e_test.go
    #entrypoint: ./test_related/run_pme_e2e_tests.sh
